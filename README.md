# HomeLab Docker Compose for Bitwarden

## Deployment
To deploy this configuration in `/usr/local/etc/vault` and store the data in `/opt/vault` run the following ansible playbook:
```
sudo ansible-playbook playbook.yml
```
